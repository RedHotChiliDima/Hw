<Window x:Class="MultiWindowDemo.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiWindowDemo"
        mc:Ignorable="d"
        Title="OrderWindow" Height="350" Width="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="пр"
        Grid.ColumnSpan="3" 
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        FontSize="16"
        x:Name="HelloTextBox"
        />
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="130"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>    
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <CheckBox Grid.Row="0" Content="Картофель и грибы" 
                      Checked="CheckBox_Changed" 
                      Unchecked="CheckBox_Changed" 
                      Tag="50" x:Name="FirstCb"/>
            
            <CheckBox Grid.Row="1" Content="Курица" 
                      Checked="CheckBox_Changed" 
                      Unchecked="CheckBox_Changed" 
                      Tag="55" x:Name="SecondCb"/>
            
            <CheckBox Grid.Row="2" Content="Вишня и творог" 
                      Checked="CheckBox_Changed" 
                      Unchecked="CheckBox_Changed" 
                      Tag="75" x:Name="ThirdCb"/>
            
            <TextBlock Grid.Row="0" Grid.Column="1" Text="50" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="55" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="75" />
        </Grid>
        <Grid Grid.Row="2" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Margin="25" Grid.Column="0" Content="Подтвердить" Click="ConfirmButton_Click"/>
            <Button Margin="25" Grid.Column="1" Content="Отмена" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
